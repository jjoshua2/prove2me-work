name: Lean check

on:
  push:
    branches: [chatgpt/spindle-axis]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Install elan
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Fetch Mathlib cache
        run: ~/.elan/bin/lake exe cache get
      - name: Compile axis glue
        run: ~/.elan/bin/lake env lean Solutions/Sol_Hirsch_spindle_one_step_axis.lean
      - name: Compile axis scratch
        if: ${{ hashFiles('Solutions/AxisScratch.lean') != '' }}
        run: ~/.elan/bin/lake env lean Solutions/AxisScratch.lean
