name: Natura scalar elimination invariant
on:
  push:
    branches: [chatgpt/natura-scalar-invariant]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean and Mathlib
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          ~/.elan/bin/lake exe cache get
      - name: Build and audit scalar elimination invariant
        run: |
          set -o pipefail
          ~/.elan/bin/lake build Solutions.CircuitEliminationInvariant 2>&1 | tee /tmp/scalar-invariant.log
          if grep -q 'sorryAx' /tmp/scalar-invariant.log; then exit 1; fi
