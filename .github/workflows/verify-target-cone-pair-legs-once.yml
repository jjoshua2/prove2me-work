name: Verify target-cone pair legs once
on:
  pull_request:
    types: [ready_for_review]
permissions:
  contents: read
concurrency:
  group: target-cone-pair-legs-${{ github.head_ref }}
  cancel-in-progress: true
jobs:
  verify:
    if: >-
      github.repository == 'jjoshua2/prove2me-work' &&
      github.event.pull_request.head.repo.full_name == 'jjoshua2/prove2me-work' &&
      github.head_ref == 'formal/target-cone-pair-legs'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - name: Check policy and pins
        run: |
          set -euo pipefail
          python3 scripts/check_actions_policy.py
          test "$(cat lean-toolchain)" = 'leanprover/lean4:v4.30.0'
          grep -Fq c5ea00351c28e24afc9f0f84379aa41082b1188f lake-manifest.json
      - name: Restore pinned Lean environment
        uses: jjoshua2/prove2me-work/.github/actions/setup-lean@main
      - name: Compile and audit target-cone pair-leg theorem
        run: |
          set -euo pipefail
          lake build Solutions.PolynomialTargetConePairLegs
          lake env lean Solutions/PolynomialTargetConePairLegs.lean 2>&1 | tee /tmp/target-cone-pair-legs.log
          python3 scripts/check_lean_axiom_log.py /tmp/target-cone-pair-legs.log \
            HirschTargetDeletion.target_slack_batch_reinsertion_with_pair_specific_cut_legs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: target-cone-pair-legs-verification
          retention-days: 14
          path: /tmp/target-cone-pair-legs.log
          if-no-files-found: warn
