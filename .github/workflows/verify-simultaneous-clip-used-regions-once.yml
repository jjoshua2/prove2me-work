name: Verify simultaneous clip used-region support
on:
  pull_request:
    types: [opened, synchronize, ready_for_review]
permissions:
  contents: read
concurrency:
  group: verify-simultaneous-clip-used-regions-${{ github.head_ref }}
  cancel-in-progress: true
jobs:
  verify:
    if: >-
      github.repository == 'jjoshua2/prove2me-work' &&
      github.event.pull_request.head.repo.full_name == 'jjoshua2/prove2me-work' &&
      github.head_ref == 'formal/simultaneous-clip-used-regions' &&
      github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - name: Check policy and pins
        run: |
          set -euo pipefail
          python3 scripts/check_actions_policy.py
          test "$(cat lean-toolchain)" = 'leanprover/lean4:v4.30.0'
          grep -Fq c5ea00351c28e24afc9f0f84379aa41082b1188f lake-manifest.json
      - name: Restore pinned Lean environment
        uses: jjoshua2/prove2me-work/.github/actions/setup-lean@main
      - name: Compile and audit used-region clip routing
        run: |
          set -euo pipefail
          lake build Solutions.PolynomialSimultaneousClipUsedRegions
          lake env lean Solutions/PolynomialSimultaneousClipUsedRegions.lean 2>&1 | tee /tmp/simultaneous-clip-used-regions.log
          python3 scripts/check_lean_axiom_log.py /tmp/simultaneous-clip-used-regions.log \
            HirschRadial.route_clip_of_lifted_endpoints_with_parent_routes_used_regions \
            HirschRadial.route_clip_with_parent_routes_used_regions \
            HirschRadial.route_clip_from_root_with_parent_routes_used_regions
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: simultaneous-clip-used-regions-verification
          retention-days: 14
          path: /tmp/simultaneous-clip-used-regions.log
          if-no-files-found: warn
