name: Natura finite phase progress
on:
  push:
    branches: [chatgpt/natura-phase-progress]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean and Mathlib
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          ~/.elan/bin/lake exe cache get
      - name: Build and audit finite phase progress
        run: |
          set -o pipefail
          ~/.elan/bin/lake build Solutions.CircuitPhaseProgress 2>&1 | tee /tmp/phase-progress.log
          if grep -q 'sorryAx' /tmp/phase-progress.log; then exit 1; fi
