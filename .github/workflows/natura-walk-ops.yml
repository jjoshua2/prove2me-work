name: Natura standard walk operations
on:
  push:
    branches: [chatgpt/natura-walk-ops]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean and Mathlib
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          ~/.elan/bin/lake exe cache get
      - name: Build and audit standard walk operations
        run: |
          set -o pipefail
          ~/.elan/bin/lake build Solutions.CircuitStandardWalkOps 2>&1 | tee /tmp/walk-ops.log
          if grep -q 'sorryAx' /tmp/walk-ops.log; then exit 1; fi
