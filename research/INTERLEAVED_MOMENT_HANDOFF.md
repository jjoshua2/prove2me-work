# Interleaved moment minimality: accepted continuation handoff

Read current STATUS, AGENTS, CLOUD_AGENT, SKILL, CONTINUE_HIRSCH and LIVE PR heads/comments before taking more work. This #287 continuation started at main9a1b6768d27d38b719bf4c0c795f8f7257415626. No accepted #285/#286 proof, #282 route work, #270 blocked companion or reserved #210 was changed or retriggered.

## Accepted proof and exact evidence

Target `Hirsch.interleaved_moment_minimal_nonfaces` is ACCEPTED, theorem380347be-2134-4160-89fc-6bf7c899f93e, submission3ba6a352-2bf8-4865-b809-79f945073a7c. The publisher's authenticated receipt records live Proved. Frozen proof47b68e63edbe28c933d6678b9f0d311f60038177, run35177182253. Actual new top-level trigger5707881466, resolved acknowledgement5707882643, verdict5707905763 were read back. Do not resubmit this accepted packet.

All three compile modes exited zero; all five axiom printouts list only propext, Classical.choice and Quot.sound. The source is623 lines. The first compiler run35176840734 failed at FOUR elaboration sites in the new assembly, not in its mathematical assumptions. One consolidated exact diagnostic repair left every statement/bound/hypothesis and both dependency bodies unchanged. The first failed run never registered or submitted a theorem. The repaired proof's FIRST ACTUAL platform submission was accepted. Separate failure source/request/diagnostics and the repair diff remain available.

Read publication_packets/interleaved_moment_minimal_nonfaces/accepted-evidence.md, the raw publication-receipt.json and packet-audit.json. All four original request/verification/publication archives and all five frozen file hashes were checked. Successful compile logs are complete; the first diagnostic excerpt is explicitly selected, not mislabelled as a full runner log. The target placeholder is separate from the admission-free proof. Live Proved is the publisher's readback, not a second direct API poll; no fresh root/leaf audit is claimed. Local Lean/Lake was unavailable; hosted pinned compilation is the specific verification evidence.

## Exact mathematical result

Let a be injective on all m original labels, and select k+1 pairs with a(l_i)<a(r_i)<a(l_j) for i<j. Use the dimension2k original moment rows, mean-centered over ALL m labels. L and R each have k+1 distinct labels and are disjoint. Every feasible x leaves some strict original-row slack in each set. Every proper S subset of either set has an actual feasible x with row_i(x)=1 exactly for i in S. Thus both sets are minimal original-H nonfaces, not merely incompatible sets.

The new sign proof indexes selected nodes by pair and Bool. Each other pair contributes two same-sign factors to a barycentric denominator, so their product is positive. The remaining partner factor is negative on the left and positive on the right. This identifies both signs without assuming a parity oracle. Accepted #286 then applies to the nonzero nonnegative original slack polynomial of degree<=2k. Accepted #285 constructs all proper-subset witnesses from the squared-root polynomial. Both complete helper namespaces were reused byte-for-byte, without resubmitting their old public targets.

Arbitrary real spacings and original-label permutations are permitted. The full original average includes unselected labels. k=0 is a valid zero-dimensional row statement, not a claim every row is a genuine polytope facet. Full-dimensionality/boundedness and a complete nonface catalogue are not in this target. Nor is there a graph-diameter upper or lower bound.

## Concurrent result and next work

A late main read found #288 accepted and merged at dfa4c0b4834fd302818646f56a0801fbbf54be75, while this request was already in progress. Its `Hirsch.moment_curve_ordered_minimal_nonfaces` proves the odd (k+1)-set in an ordered chain of2k+3 selected nodes. This theorem covers BOTH interleaved sides with2k+2 selected nodes. They are distinct but substantially overlapping interfaces; do not claim two separate general breakthroughs. All #288 files are preserved unchanged. Neither packet needs a duplicate submission.

For the integer parameters0,...,4k, every k+1 odd-label subset has the even predecessor separators s_i-1. This supplies an immediate mathematical application of this theorem. The exact future formal task is to enumerate those subsets, prove distinctness and their binomial count, instantiate the accepted minimality result for each member, then connect the registry to the accepted stellar persistence/count theorem. Any polytopal interpretation must retain its actual geometric hypotheses. This remains an obstruction to the TOTAL size of full forward flagification, not to short original-edge routes.

The general Polynomial Hirsch task is a polynomial ordinary-edge upper bound for arbitrary carriers. These results do not furnish that bound and should not be turned into another open child merely restating minimality or assuming cheap global completion. Prefer a genuinely new adaptive face/route argument after completing the necessary precise formal assembly.

## Exact tests and reproducibility

The committed script test_interleaved_moment_minimal_nonfaces.py tests150 configurations:72 random rational/label-permuted cases, all76 odd-subset applications for k1..4, and selected dimension16/32 cases. It constructs4782 proper-subset witnesses and checks72414 all-row identities,9564 sign/slack conclusions,1120 weights and970 moment identities. Large full families are not enumerated. Three invalid order/injectivity inputs are rejected; a feasible adjacent-pair example shows that dropping interleaving changes the conclusion.

A clean standalone-script replay reproduced the FULL report byte-for-byte. Source SHA256302497b606f5733e450cd2bc21903bb4b763b96e42df60ee49717e8cbf836c58, Git blobe68de222a1c873463301a252d0df93af1c00324c. Report SHA25683c1e84d699d056afc1cb5504256e597b857357f9afc8b5d2ca157ab932e205f. Accepted dependency artifact hashes, exact namespace reuse, failed and successful request records are preserved under research/verification/interleaved-moment/.

    python3 scripts/test_interleaved_moment_minimal_nonfaces.py --out /tmp/interleaved-tests.json

The downloadable export includes original artifact archives and full raw evidence. These Python tests are not Lean-extracted instance checks or a proof of universal real claims. Protocol0.10.4, the committed Lean4.30.0/Mathlib pin, workflows, allowlist, duplicate guards and verify/publish secret separation are unchanged. Root STATUS was not replaced underneath concurrent indexing; this handoff and a completion cross-reference record the accepted interface.
