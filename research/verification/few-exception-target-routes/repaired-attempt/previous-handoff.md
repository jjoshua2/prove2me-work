# PR #325: few-exception original-row routes — first gate failed

## Current lifecycle and exact request

Keep this existing PR OPEN/DRAFT. The complete new public theorem is NOT verified.
Do not merge as accepted, rename the target or repeat the first publication request.
Read STATUS.md, AGENTS.md, CLOUD_AGENT.md, SKILL.md, CONTINUE_HIRSCH.md and live
heads/comments/ownership before resuming. Their older frontier summaries can lag
actual accepted receipts; #323 and #324 are already accepted and merged.

Repository: jjoshua2/prove2me-work.
PR: #325; branch: proof/few-exception-target-routes.
Target: Hirsch.few_exception_target_original_routes.
Packet: research/publication_packets/few_exception_target_routes.
Tested proof: ccf39230fac5e32cf352ccdf853acbeba558f781.
Base/trusted main: 34696607bb8f6298bb400d9ce9a0903dc078c157.

Coordination on #324: 5769262108. Actual NEW top-level publication request:
5769409206, posted 2026-09-22T00:14:45Z. Bot acknowledgement 5769410615 resolved
the exact tested proof to run 35671141114. The command and acknowledgement were
read back by ID. This was ONE hosted request, not an interactive compiler loop.

Gate 106567634984 succeeded. Verifier 106567673751 failed complete driver
compilation with exit 1. Separate solution and target-statement compiles were
not reached. Report-verify 106568033078 and publish 106568033516 were skipped.
No complete packet audit, verified-packet archive, registration, proof submission,
theorem/submission ID, ACCEPTED or Proved result was produced. There was no
independent platform or mission-root poll. No post-gate proof/metadata edit or
additional request occurred; later branch commits contain evidence only.

## Mathematical result and its exact limits

For exact P=conv(C)=the original m halfspaces in ambient d and actual extreme
endpoints u,v, let B be ANY finite set of original labels and k=|B|. Only rows
tight at target v and outside B must take their boundary value and at most one
other value on all actual vertices. The complete written argument and Lean
candidate construct an original ordinary-edge route with

    L <= m-d + ((m+1)^k-1),

preserving ALL acquired target rows, including exceptions. This is polynomial
in original m for EACH FIXED k, with a degree depending on k. It is not uniform
Polynomial Hirsch, an efficient H-to-V or polynomial-time implementation, a
shortest-path theorem, or all-facet nonrevisiting. #324 remains sharper at k<=1.
No historical-priority or best-known-bound claim is made.

First acquire good two-level target rows via actual improving edges in the
retained target face. This prefix costs at most m-d. The remaining direction
space N annihilates those fixed good rows. Evaluation on B is injective by the
target active-kernel theorem, deriving dim(N)<=k without a rank premise.
At each actual remaining vertex select at most dim(N) ORIGINAL active rows
spanning their restrictions to N. These determine the vertex uniquely in the
residual affine space. Encode their labels in k optional slots, deriving at most
(m+1)^k remaining vertices. Strict target-objective ascent constructs a genuine
locked original-edge finish of at most that count minus one. Append the walks.

No graph, neighbor, active basis, residual dimension, small vertex catalogue,
connectivity, cheap residual phase or short-route oracle is assumed. Exact
finite-hull/H equality and two-level structure outside B remain explicit.
Empty B, all-row B, nontarget exceptions, redundant/interior generators,
redundant rows, nonsimple/lower-dimensional hulls, d0 and equal endpoints remain.
Public edges are whole nondegenerate IsExtreme original segments; local exposure
is not silently strengthened to a separate original-ambient exposure theorem.

Residual edges need not acquire a new row. The explicit two-exception hexagon
from (0,0) to (2,2) has two initially missing target rows but distance three and
no acquiring source neighbor. It prevents copying #324's stronger acquisition
claim; it is not a diameter counterexample.

## Actual two compiler diagnostics and saved repair

Original source: 1264 lines / 55478 bytes; blob
eb9d1ac5034e94b384c708b76c3da7bc463845c9; SHA256
2c4d5644108d23af82fcd2cd472863d5913d5522e8d996a850182f702e5a5301.
All three remote packet blobs match the prepared bytes. Accepted #323's
844-line / 35088-byte namespace prefix is unchanged; its old public root and
prints are omitted, not resubmitted. All five dependency manifest digests pass.

At original line 965, `congrArg Subtype.val he` infers the wrong subtype in the
injection J -> S. The proposal explicitly writes `(fun s : S => s.val)`.
At original line 1011, `split_ifs at hi with h` has already eliminated the
impossible None=Some case. Remove only the final `cases hi` branch. Keep the
first-direction equality proof following `rw` at line 1005; it is NOT the
reported redundant tactic. Numbered-source review corrected that initial
local drafting mistake before any repair upload or submission.

The compiler emitted standard-only reports for residual_dimension and
enter_residual. The five other reported declarations, including solution,
contain sorryAx from failed elaboration. No admission was written, but this is
NOT a passing complete proof or transitive audit.

The saved proposed-local-repair.patch is UNAPPLIED to the publication source
and UNCOMPILED. Proposed source: 1263 lines / 55471 bytes; blob
0dda4d16f141430683f475519c3028f8125af7ca; SHA256
10a7817b6e429bdc4254e25deb385a02cc5dae8a7c7afd4335fc06f6306aac08.
All declaration signatures, accepted-prefix bytes, metadata and the complete
public root statement AND proof body remain unchanged. Forward/reverse patch
application reproduces both complete files exactly. These are source checks,
not Lean compilation, and further errors may appear after applying the repair.

Lean 4.30.0 was observed in the hosted setup. Mathlib remains
c5ea00351c28e24afc9f0f84379aa41082b1188f. No Lean/Lake/cached toolchain was found
in PATH and checked local locations, and toolchain-host DNS failed. No local
compiler or direct authenticated platform poll is claimed.

## Evidence, exact tests and reproducibility

Evidence directory: research/verification/few-exception-target-routes/.
It preserves the original failed source, complete compiler error/axiom block,
exact resolved request, source/dependency/replay/test records, full small/large
reports and malformed-control output, and the separate repair proposal.
The decoded verifier runner log was read through cleanup; the saved transcript
removes runner timestamps and is NOT a full raw runner archive. There is no
missing verified-packet or individual API response to reconstruct or invent.

The only artifact was request 10669999660, 307 bytes, SHA256
378dd768e2d56f2c9ba129906548818cfefdb772c4ceeaac6795d833baca2b78.
It was downloaded and independently rehashed; its original ZIP is in the export.

The new script test_few_exception_target_routes.py uses two existing unchanged
reference scripts. Small tests cover 23 hulls, 160 actual vertices, 165 displayed
rows and 160 targets, including 80 outside #324's one-exception premise.
There are 434 checked residual vertex codes and 1402 routes / 2137 original-edge
occurrences versus 2130 shortest-edge occurrences. Seven nonshortest routes and
12 nonacquiring steps remain. All-row exception boundary checks are separate.
All stored route/code consumers replay with production disabled.

The four multi-roof examples d/k=8/2,16/2,32/3,64/4 certify 120 further original
edges with exact original rows and sparse right inverses, without large graph
discovery. Their k independent exceptional rows each have 2^(d-k)+1 global
levels by a WRITTEN family formula, not large enumeration or an additional Lean
instance theorem. The general bound is much looser than these explicit routes.

Twelve genuinely malformed controls fail. An initial false-acquisition edit
was a no-op on the chosen hexagon edge; it was corrected before the final suite
and is not recorded as a successful initial rejection. A clean THREE-script
workspace reproduced both full reports, both full fixtures and the control
output byte-for-byte. Full fixtures (2399500 and 331592 bytes) accompany the
export and regenerate. All five output hashes were rechecked after the gate.
Tests and JSON processing are not kernel-verified or a runtime complexity proof.

    python3 scripts/test_few_exception_target_routes.py --out /tmp/few-exceptions

## Next action and ownership

Resume THIS draft and the saved two-site repair after checking live ownership
and pending submissions. Apply/review the exact patch, compile locally where
available, then obtain a complete pinned compiler/axiom gate before publication.
Do not weaken the type, rename a duplicate target, resubmit accepted #323/#324,
or rerun this failed frozen source. No extra trigger was made this continuation.

At the mission level, the uncontrolled exponent k remains. A tighter subset
count alone does not yield a uniform-degree polynomial; a future route argument
must control the relevant exceptional geometry or avoid enumerating its states.
Respect #282's distinct triangular work, reserved #210 and all other ownership.
No pins, strict0.10.7 version guard, workflows, actor allowlist, duplicate guards
or secret-free verifier / trusted publisher separation have changed.
