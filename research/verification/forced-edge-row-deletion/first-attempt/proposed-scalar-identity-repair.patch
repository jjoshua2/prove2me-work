--- a/research/publication_packets/forced_edge_row_deletion/solution.lean
+++ b/research/publication_packets/forced_edge_row_deletion/solution.lean
@@ -198,7 +198,8 @@
         simp only [map_add, Finset.sum_add_distrib]
       map_smul' := by
         intro c x
-        simp only [map_smul, smul_eq_mul, Finset.mul_sum] }
+        simp only [map_smul, smul_eq_mul, Finset.mul_sum]
+        rfl }
   have hle : ∀ z ∈ relaxed A b keep, ∀ i ∈ T, A i z ≤ A i u := by
     intro z hz i hi
     have ht := (Finset.mem_filter.mp hi).2
