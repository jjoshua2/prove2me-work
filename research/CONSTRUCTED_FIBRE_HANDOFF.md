# Actual fixed-core fibre routes, not another scalar sequence theorem

Packet: research/publication_packets/constructed_fibre_routes/.
New target: Hirsch.constructed_minkowski_fibre_route; PR #244.
Corrected proof head:3911e9440cf122a1693b0844f213c5973a9eb9d4.
Corrected gate:34806444523; read its actual outcome before claiming verification.

The first gate34805680795 failed at exactly three beta-normalization rewrite
sites. The repair adds one explicit change to a lambda-applied equality and
uses congrArg for the two translated endpoint equalities. Every public/helper
statement, assumption, budget and geometric conclusion is unchanged. The first
failure remains in CONSTRUCTED_FIBRE_FIRST_GATE.json and registered/submitted
nothing. Source hash05cd2756d449434c21fca91b5750e2edce99d557129808645307ac3a76076b9d.
No further speculative compiler loop or theorem weakening is authorized by
these targeted elaboration repairs.

The concurrent #243 scalar sequence is ACCEPTED and merged. Its exact repaired
source is reused, not an assumed child: SHA-256
c26a8737a7d2b3c697b9b5a197f0733b3129f2ad60b16071adb6f15e3a5ec8dc,
run34804405107. The independent duplicate scalar proof was withheld and never
pushed or resubmitted. This new result binds that sequence and accepted generic
objectives to whole-sum exposed edges while preserving the core comparisons.

The new geometric theorem constructs route points and full supporting slices
in P+sum_i conv(S_i), keeping the same exposed core point fixed. Its cost is
sum_i(k_i-1), independent of the number of core points. Finite presentations
and strict endpoint exposures remain actual inputs; samples, walls, maximizers,
genericity, support-segment identities and the path/count are constructed.
It is not a claim of arbitrary-carrier decomposition or a dimension-uniform
bound in the original facet count.

Three bundle-only reusable module candidates are supplied: the scalar core,
a sequence-output adapter and the constructed fibre-route module. They are not
separately committed or module-build-verified. Do not overwrite existing
PolynomialMinkowskiExposedEdges or PolynomialEnvelopeCrossingEdges with local
partial dependency copies; those only flatten the standalone Mathlib packet.

Run the committed exact regression:

    python3 scripts/test_finite_envelope_sequence.py

It checks independent dominance intervals, stationary-transition provenance,
complete small finite sum support faces, full core support slices, large cases
without tuple-product enumeration and thirteen rejected malformed controls.
The large 3^r figures are raw tuple counts, not asserted distinct vertices.
Python and JSON are not Lean-extracted. The runtime lacks local Lean/Lake;
only actual pinned compiler logs and authenticated verdicts establish their
respective statuses. Supporting code is never executed by the packet gate.

Next, concatenate fixed-core legs with #240's actual bridges using the SAME
chosen endpoints and cost L+(L+1)*sum_i(k_i-1). This final core-route assembly
is not proved by the current public theorem. An arbitrary H-carrier still
needs a useful finite representation and a controlled core route.
#241 remains independently blocked before its publication comment this turn;
its source is not part of this packet or an alternate trigger attempt.
