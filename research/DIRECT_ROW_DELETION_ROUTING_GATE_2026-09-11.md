# Direct row-deletion routing gate

This branch contains the two theorems in `Solutions/PolynomialDirectRowDeletionRouting.lean`.

The shared temporary verifier was narrowed to this branch and the cap-functional branch after earlier structural modules had already been independently gated and merged. This file only retriggers the targeted hosted compile/axiom audit; it changes no theorem source.
