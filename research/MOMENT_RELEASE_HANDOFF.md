# PR #298: arbitrary moment-vertex release pivot — ACCEPTED

Read live STATUS, AGENTS, CLOUD_AGENT, SKILL, CONTINUE_HIRSCH and current PR
heads/comments before taking work. This continuation began at main9d1994b8a27b505455179777c5e99d08ec49bc05,
branched fromad7284ebacca004ab48987796c0d1b34e85822e6, and left all other owned
branches and reserved #210 untouched. #296 consecutive-block routes and #297
uniform catalogues advanced concurrently and must not be resubmitted.

## Completed proof and evidence

`Hirsch.moment_vertex_release_pivot` is ACCEPTED, theorem
d86d447e-5259-4d8c-b963-6b8102e1e5be, submission8dd2f7b2-d9fa-436a-8378-e691d3e3dba4.
The trusted publisher records authenticated live Proved. Frozen proof:
a9fab43328ccef743d8a2ac7b1ec18a6f987e001. Successful run35270640502, trigger5720744076,
resolved acknowledgement5720745968, verdict5720794068. Do not resubmit.

All three compilation modes exited zero. Five transitive axiom reports contain
only propext, Classical.choice and Quot.sound. The full754-line solution keeps
the complete544-line accepted #295 prefix byte-identical and omits only its old
public solution/print suffix. #295 includes the accepted #293 active-value and
vertex criteria. The dependency artifact and all its frozen hashes were checked.

The first driver compilation in35270103589 failed at ONE rewrite site. The exact
lambda application shown by the compiler was made explicit with change before
rewriting. No helper/public type, hypothesis, bound, metadata or accepted prefix
changed. First-gate source/request/diagnostics and the patch remain under
verification/moment-release/. That run had no registration/submission. The
corrected source passed the second compiler gate and first actual platform
submission, not the first compiler gate. No third trigger or proof edit occurred.

See the packet's accepted-evidence.md, raw publication-receipt.json and audit.
All four original archives and all five final frozen file hashes were checked.
Raw evidence and derived readbacks are separate. The publisher job was followed
through verdict, upload and comment; final gate/verify/publish succeeded and
report-verify was skipped. Proved is the publisher's authenticated readback, not
a separate direct API poll. No fresh root/leaf mission poll is claimed. Local
Lean/Lake was unavailable; pinned hosted compilation is the actual evidence.

## What the theorem constructs

Use the original d-dimensional mean-centered moment inequalities with d<m and
injective real node values. From ANY actual extreme point u and ANY tight row p,
active-value surjectivity supplies w with A_p(w)=-1 and all other current active
values zero. The sum of ALL centered row slopes is zero, so some slope is positive.
Every positive slope belongs to an originally slack row. Minimize the positive
finite ratios (1-A_i(u))/A_i(w), obtaining t>0 and a blocker q.

All original rows are feasible on [0,t], and q excludes every s>t. At v=u+t*w,
the old d-1 retained rows and q are tight. The accepted global bound on tight-row
cardinality forces them to be the entire active set. The accepted vertex criterion
and common-slice theorem yield a distinct actual extreme point and an ENTIRE
exposed original edge, not an auxiliary projection. The public type also records
the exact normalized direction, minimum ratio and full nonnegative ray interval.

No direction, rank, inverse, neighbor, blocker, step length or path is a supplied
premise. d0 has no active p and is vacuous; d1 works. Arbitrary real spacings and
label permutations are allowed. Irredundancy of all m rows in every boundary case
is not asserted. Classical simplex geometry is not claimed historically new.

## Remaining quantitative target

This closes the missing second-endpoint construction after #295. A future policy
may choose a leaving row and then use this genuine original-edge step, but this
theorem does NOT prove polynomially many choices reach an arbitrary target,
monotonicity, cycle prevention or shortestness. It must not be repackaged as a
short-route theorem by assuming a short exchange sequence. The consecutive-block
case is separately handled by #296. General original-edge routing still requires
a justified policy and quantitative bound, and arbitrary carriers need more than
this moment-specific rank structure. Polynomial Hirsch is not solved here.

## Exact code and reproducibility

The script scripts/test_moment_release_pivots.py independently enumerates six
small original-H graphs:194 square systems,69 vertices,272 outgoing pivots,
4934 inverse identities and4630 full-reference objective comparisons. The125
infeasible full-tight systems remain excluded. Each delivered common-row objective
maximizes only at the two checked endpoint vertices in the complete references.
Nine additional pivots in dimensions8/16/32 evaluate all original rows without
large graph enumeration. The polynomial seed there is an instance generator,
not a duplicate block-route proof. Fifteen saved records audit with make/inverse
production disabled; nine malformed records fail.

Source blob838a57f82e518a921b34012508846a9a383e62a1,
SHA25697d9c98924b9c31d6f6101b226bf15ac3b1485a56b72786cd21ec604a8ab2bd0.
The clean standalone replay reproduces the complete3390-byte report and263614-byte
fixture exactly: report9e1b440c5b296c9c42f1ab66bc95670fff7f4b8199204de6afdd945cb44c8166,
fixture84a53cffb5be786fb812dd6a66d78bfe8049bdfe18353105101f3ea83c4929e1.
Supporting tests are not Lean-extracted or a kernel certificate for arbitrary JSON.
The full fixture and original archives accompany the export and regenerate with:

    python3 scripts/test_moment_release_pivots.py --out /tmp/moment-release

Root STATUS was not overwritten under concurrent indexing. This durable handoff
and the completion comment record the accepted interface. Lean4.30.0/Mathlib
c5ea00351c28e24afc9f0f84379aa41082b1188f, protocol0.10.4, workflows, allowlist,
duplicate safeguards and trusted verify/publish secret isolation remain unchanged.
