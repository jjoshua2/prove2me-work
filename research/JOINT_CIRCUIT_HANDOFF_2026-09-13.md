# Joint-circuit support handoff: ACCEPTED and live Proved

## Completed proof and delivery

PR #235 delivers the previously local joint-circuit support proof and exact Python regression. Public theorem `Hirsch.joint_external_circuit_support_and_zero_deficit`, ID `215a5bbf-fda4-467b-9e99-8714e6ec007b`, submission `108c2171-0be9-48bd-a1f2-191f35f8b6fc`, is ACCEPTED with authenticated publisher live Proved. Do not resubmit it.

The 197-line solution and exact problem are unchanged from the originating local bundle. First prepared run `34791096717` at frozen proof head `e78b966247f66bbe3906851395a1fd63bfb243b0` passed driver/solution/statement compilation and all four standard-axiom printouts, then its first authenticated submission. Solution SHA-256 `3eecdf37320f91cd50466721cd0c12685d45351f5b3e05370af7377483ad3f51`. The comment trigger was actually posted and read back; it was not a workflow_dispatch.

The source here is the standalone publication packet, not a separately built Solutions module. The earlier local named mirror does not gain an independent module-build claim from this packet gate. Local Lean/Lake is unavailable; successful compilation evidence comes from the pinned hosted gate, not Python/static checks. No repeat speculative Actions run was made.

See `publication_packets/joint_circuit_support_deficits/accepted-evidence.md`, raw `publication-receipt.json`, `manifest.json`, `packet-audit.json` and compile logs. Both artifact archives and all five frozen hashes were checked. Derived readback summaries are labeled separately from raw records. Source/proof files were not changed by post-gate evidence commits.

## Exact mathematical gain

For an external support-minimal nonnegative null multiplier of actual independent simplex-block allocation rows, every block has a zero auxiliary coordinate. Therefore its total multiplier is the attained maximum of zero and the combined original-row evaluations. Nonnegative candidate support bounds give nonnegative deficits Gamma. Gamma=0 iff one listed candidate point (including the anchor) attains every positively weighted row bound. Empty/repeated/dependent lists are included.

Internal-only rows are automatic for nonnegative scales and must not be claimed to have nonnegative deficits. Externality without support minimality is insufficient. The exact nonminimal counterexample has (lambda,nu,mu)=(1,2,1), a=h=1, deficit -1. See the full argument in `JOINT_CIRCUIT_SUPPORT_2026-09-13.md`.

The exact `scripts/check_joint_circuit_support.py` is now COMMITTED, Git blob `52082e256da8dcf0b09b5189a8c61ece185168b1`, SHA-256 `0ab192816af732c9f3fa36a944f6f722b17803e3c1bfa13223eb1c3ee04ea5e7`. Its rerun: 59 systems, 3509 support candidates, 267 circuits, 267 independently certified ranks, 278 block support checks, 556 zero tests, 549 downward checks, 354 tightening cases, 11 rejected controls and 60 determinant comparisons. The receipt is `JOINT_CIRCUIT_SUPPORT_CHECK_2026-09-13.json`. These are finite software checks, not universal Python/JSON correctness or Lean extraction.

## Next connected work

Read current STATUS and LIVE PR ownership first. Accepted #229/#233 provide the actual checked catalogue and all real dual tests. #234 is now the accepted covering-budget allocation alternative; it was completed by the other line, not by this PR. Do not rewrite or republish either ingredient.

Compose their actual coefficient and index interfaces, not an assumed completeness/Farkas/pointwise equivalence. Transport the catalogue's real support-minimality predicate to the original+block coordinates before applying #235. The weak public nonnegative-null invariant in #219 does not imply minimality. For separate simplex blocks, rho=1 gives coverage in #234. General overlapping budget rows require their actual allocation image and do not automatically represent independent Minkowski factors.

For each external output ray, #235 derives the packing coefficients and identifies zero ones. The existing geometric scale-region lemma then supplies downward closure once the complete geometric equivalence is established. Use original-H primal/dual sharp support witnesses for exact right-hand-side budgets; a conservative upper bound supplies only sufficiency. The remaining source-level complete adapter, useful shape selection and arbitrary high-dimensional ordinary-edge routing are not proved by this sign theorem.

#210 was not modified or triggered. Its retired ancestry is not imported. #208's existing pending submission is not duplicated. No Lean/Mathlib pin, workflow, permission, allowlist, credential, or secret-separation change was made.
