# Constructed fixed-core fibre route: compilation and audit passed

The corrected1477-line standalone solution at
3911e9440cf122a1693b0844f213c5973a9eb9d4 passed pinned Lean4.30.0 verification
in run34806444523. Driver, solution and statement exit codes are zero. All five
printed declarations depend only on propext, Classical.choice and Quot.sound.
The actual endpoint and full-sum edge theorem is included, not only its helpers.

The first gate34805680795 isolated three beta-normalization rewrite sites.
One explicit change at the injectivity equality and two congrArg translations
repair those exact sites. Every public/helper statement, assumption, factor
bound, zero-step membership and exposed-edge conclusion is unchanged. The
original failure is preserved in research/CONSTRUCTED_FIBRE_FIRST_GATE.json;
it registered/submitted no theorem. This is a targeted diagnostic repair,
not a changed theorem or a claim that the original failed proof was verified.

The original verified archive10333750010 was independently downloaded and
hashed as20dd92650c80f0d732f06adf2850c9535feac52972ab9820eeb968bc7ee9b830.
All five frozen content hashes were recomputed; solution, problem and explanation
match the submitted files byte-for-byte. packet-audit.json and
verified-artifact.json are unchanged downloaded records. artifact-readback.json
is an explicitly derived local check, not a fabricated platform response.
The original ZIP and full successful compiler logs are in the conversation bundle.

The source is a complete existence theorem for fixed-core fibre routes in the
FULL Minkowski sum. Its count charges only added factor point lists. It reuses
accepted #243 scalar sequence, #242 genericity, #239 count and compiled crossing
proof bodies; it does not resubmit the scalar theorem. The standalone packet
is the verified artifact; three bundle-only reusable module candidates have no
separate module-build verdict and must not overwrite older partial dependencies.

The supporting rational regression and source-hashed receipt are committed.
Candidate Python is not executed by the verification/publish workflow and is
not Lean-extracted. Local Lean/Lake is unavailable; compilation happened in the
pinned hosted environment. The platform publication job is SEPARATE: this
intermediate note does not imply ACCEPTED. Read the final trusted receipt
before any resume, duplicate registration or acceptance claim.
