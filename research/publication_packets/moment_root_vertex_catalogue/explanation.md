# Complete root-polynomial reconstruction, including negative means

The preceding accepted #293 theorem characterizes an actual original moment vertex by feasibility and exactly d tight original rows, and constructs active-row evaluation inverses. This packet uses those proofs to REMOVE the need for a supplied feasible vertex or a vertex table in the finite reconstruction. It does not resubmit #293 and does not overlap the consecutive-block route construction in #296 or the generic cut-coordinate overcover in #297.

For every d-element original-label subset S, form the monic polynomial q_S(t)=product_(i in S)(t-a_i), and let mu_S be its mean over ALL m original labels. Put v_S(j)=-coeff_(j+1)(q_S)/mu_S. For mu_S nonzero, the accepted centered-evaluation identity gives exactly row_i(v_S)=1-q_S(a_i)/mu_S. Since parameters are injective, q_S(a_i)=0 exactly on S. Hence nonnegative normalized values at all original nodes imply feasibility, exactly S tight, and therefore actual extremality. Distinct accepted subsets cannot yield the same point, since the point's exact active set recovers its index.

The substantial reverse step shows that every actual vertex appears in this EXPLICIT filter. Let S be its exact d-row active set. If mu_S=0, the nonconstant coefficients of q_S define a vector annihilated by every active original row. The accepted active-kernel theorem forces that coefficient vector to vanish. Centered evaluation would then make q_S vanish at every original parameter, including a label outside S, which exists because d<m. That contradicts the exact root set. Thus mu_S is nonzero, without assuming a positive leading sign or a generic configuration. The candidate matches all active values of the original vertex, and active-evaluation injectivity forces equality.

The finite catalogue is defined using powersetCard d univ and the nodal sign/mean test only. Its image is EXACTLY all original extreme points, not an overcover. It has no duplicates, and its cardinality is at most choose(m,d). That binomial bound can be exponential; this is not a uniform polynomial algorithm or a new graph-diameter bound. The filter on arbitrary real data is noncomputable Lean mathematics, not a formally extracted rational implementation or a JSON certificate checker.

Negative means MUST be allowed: the root polynomial for labels {0,4} among integer parameters0,...,4 is nonpositive on all original nodes, with negative mean, and still gives a valid 2D vertex. Discarding it would make the catalogue incomplete. Dimension zero works with the empty product and the unique zero-dimensional point. Both odd and even dimensions, scrambled original labels, redundant original rows in boundary cases, and arbitrary injective real parameters are retained. Genuine-facet status of every original row is not an additional conclusion.

The full accepted namespace prefix from #293 is reused byte-for-byte, with its previous public root omitted. The new public signature uses only Mathlib and explicit let formulas. It assumes neither a root-polynomial criterion nor a rank oracle; both directions are proved. Five transitive axiom reports cover the new identities, nonzero-mean derivation, reconstruction, catalogue, and public root.

Local Lean/Lake is absent in this runtime and the toolchain-host DNS preflight fails. Source checks and exact rational regression are not compilation. The complete candidate uses one requested NEW top-level PR publication comment for the existing pinned verification/axiom/publication gate. On a real failure retain diagnostics and draft status rather than repeating speculative hosted edits. No accepted/pending packet, other agent's source, pin, workflow, allowlist, duplicate guard or trusted secret separation is changed.
