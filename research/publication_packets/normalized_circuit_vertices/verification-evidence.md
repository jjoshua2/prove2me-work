# Exact hosted verification evidence

The first prepared gate compiled the unchanged proof at
`cfb93bd2618cf153f8eb00b11918be3ae874b9a7` in Actions run `34783508448`.
The audit driver, solution and statement all exited0; the four actual proof
printouts use only `propext`, `Classical.choice`, `Quot.sound`.

The artifact archive10324494880 was downloaded and its SHA-256 independently
checked: `70529b4f2df3ff1a43e58d17165654a77c0936a8e3dae12760ece7301caa71b3`.
All five frozen hashes were recomputed; the original solution, problem and
explanation are byte-for-byte equal to the frozen copies. The raw audit and
artifact summary are preserved as `packet-audit.json` and
`verified-artifact.json`. `artifact-readback.json` is an explicitly derived
local check record, not a fabricated server response. Full compiler logs and
the original archive are in the downloadable conversation bundle.

The publication job is separate. Compilation/audit success does not imply
platform acceptance. Consult the PR's final trusted verdict/receipt; do not
resubmit merely because a pending run has not finished within a chat turn.

The companion Python upload was blocked by the tool's safety-status check.
No alternate encoding or write route was attempted. Three exact-rational
scripts and their executed tests exist in the downloadable bundle ONLY, not
as committed Python or Actions-executed candidate code. The reproduction
commands in explanation.md refer to that complete bundle. The proof packet
and its verified source are unaffected by that independent upload block.

Historical conformal decomposition #25 is already accepted and was not
resubmitted. #221's support-optimality and #222's rank work are separate;
#210 and all its assigned-agent work remain unchanged and untriggered.
