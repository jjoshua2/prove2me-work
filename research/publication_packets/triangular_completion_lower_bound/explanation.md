# Exponential intrinsic completion cost from a linear-size original H system

Fix 0<e<1/2 and dimension n+1. The original inequalities are
0<=x_n<=1 and e*x_(i+1)<=x_i<=1-e*x_(i+1), for i<n. There are exactly 2(n+1) displayed inequalities. No enlarged generator or corner list is used as the original size parameter.

First prove this minimal presentation equals the accepted triangular feasibility predicate, deriving the redundant coordinate box bounds backwards from x_n. Recursively interpolate each feasible head between the affine lower and upper lifts of its tail. The lifts preserve convex combinations, so induction proves that the ENTIRE H body is the convex hull of the actual recursively constructed Boolean corners. This equality is a conclusion, not a supplied vertex-catalogue oracle.

For each bit string b of length n, fix the first n coordinates to the indicated lower or upper boundary and leave x_n=t free. The resulting affine line segment has endpoints t=0,1. A recursively constructed linear functional, the sum of those chosen ORIGINAL rows, is bounded above on the entire feasible body. Equality forces each selected row to be tight, hence forces the point onto exactly that line segment. It is therefore a whole nondegenerate exposed edge, not merely a feasible chord.

The endpoint displacement has last coordinate 1. Each preceding component equals plus or minus e times the following component according to its boundary choice. Every component is nonzero. Two parallel displacements must have scaling factor 1 by their last coordinates; recursive cancellation then recovers every bit. Thus the 2^n bit strings give 2^n pairwise distinct unoriented genuine original edge directions. The construction does not need the stronger count of ALL edge directions.

Apply the accepted #319 direction-inheritance theorem to the derived finite hull and selected edge family. Every nonempty compact-summand zonotope completion must contain at least 2^n different generator directions. Its accepted intrinsic direction-count theorem constructs opposite actual completion vertices requiring at least 2^n steps on every path through whole nondegenerate exposed segments. This lower bound concerns the same original completion, not a cube preimage or an arbitrary projection. Q convexity is unnecessary in the stated theorem; compactness, nonemptiness and actual whole-set sum equality remain hypotheses.

The two accepted namespace sections are reused byte-for-byte. Their previous public targets are omitted, not resubmitted. New source includes the full hull, support, direction and public assembly proofs with no admissions or changed axioms. A top-level solution matches the exact import/open/settings-only target. The committed Lean4.30.0/Mathlib pin is unchanged.

This formally targets the concrete family application left written after #319, using the sufficient lower bound 2^n rather than 2^(n+1)-1. It does not include a Mathlib facet-lattice count, exact original shortest diameter, an asymptotic domination theorem, translation transport or a Polynomial Hirsch counterexample. The original family has short cube routes classically; the obstruction is to uniformly cheap GLOBAL zonotope completions. Charging only nonstationary contracted steps, non-zonotopal constructions and other original-edge arguments remain outside it. Classical triangular/Klee-Minty background is credited to Gaertner--Helbling--Ota--Takahashi (arXiv:1308.2495); no historical-first or new best-bound claim.

Local Lean/Lake is unavailable and toolchain-host DNS fails. Source consistency and exact-rational regression are not Lean verification. This complete packet is prepared for one normal new top-level PR publication comment, with full pinned compilation and transitive axiom audit required before registration/submission. Preserve any actual failure without speculative repeated hosted edits or weakening the target. No pin, workflow, strict protocol guard, allowlist, duplicate safeguard or verify/publish credential isolation is changed.
