# From actual local pivots to a complete all-endpoint route

The accepted release-pivot theorem constructs a genuine original edge for any chosen tight leaving row, but does not select a row giving progress toward a target. The accepted root catalogue lists every original moment vertex, but does not connect them. This packet proves the missing selection and complete finite route, without assuming either an improving-step oracle or a short path.

At a source vertex u, for each tight row p choose its accepted normalized direction w_p, with row_p(w_p)=-1 and the other source-active values zero. For a feasible target v put c_p=1-row_p(v)>=0. Active evaluation proves exactly v-u=sum_p c_p*w_p. Thus whenever a linear objective f satisfies f(v)>f(u), at least one summand has c_p*f(w_p)>0. Both c_p and f(w_p) are positive. The first-blocking edge in this direction strictly improves f. If a target row is already active at u then its coefficient c_p is zero, so this chosen positive-coefficient release cannot remove it. Every such row remains tight at the next vertex.

Take f to be the sum of the original rows tight at v. Feasibility bounds every summand by one; equality of the full sum forces every target row tight. Active-evaluation injectivity at v then forces the point to be v. Hence every distinct actual vertex has a strict score gap and an improving original edge preserving acquired target rows.

The exact finite root catalogue from #299 supplies all actual vertices, not an assumed inventory. On that finite set, count the vertices with score strictly greater than the current score. An improving edge strictly decreases this natural rank. Strong induction constructs the whole route to v with fewer edges than the catalogue cardinality, hence fewer than choose(m,d). The zero-step case is explicit. Only the prefix through L of the returned natural-indexed function is the route; no infinite path is asserted.

The strict fixed score rules out repeated vertices in this prefix and makes every exposed segment nondegenerate. The per-step target-row implication preserves every already acquired target row. Non-target rows may still be revisited, and shortestness is not claimed. The binomial bound may be exponential; this does not close Polynomial Hirsch or improve known cyclic-polytope diameter bounds. It is a complete formal all-endpoint monotone interface, including negative-mean/nonconsecutive vertices and odd dimensions, distinct from the owned separated-pair polynomial construction #300.

The accepted source through Hirsch.MomentRelease and the full Hirsch.MomentRootCatalogue namespace are reused exactly, deduplicating their identical common prefix and omitting only old public roots/printouts. The new public preamble contains only Mathlib import/open/settings and its type uses explicit original formulas. Five new axiom printouts cover the improving edge, strict target objective, finite ascent, all-endpoint assembly and public root.

Local Lean/Lake is absent and fresh toolchain-host DNS failed. Source inspection and exact rational tests are not Lean verification. One complete new top-level PR conversation comment requests the existing pinned compiler/axiom/publication gate. No speculative hosted edit loop, old accepted resubmission, workflow, protocol, allowlist, pin or verify/publish secret split change. Any actual failure must remain in the handoff.
