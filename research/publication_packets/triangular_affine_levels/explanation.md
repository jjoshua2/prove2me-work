# Actual triangular extreme points and the affine-level count

This fills the constructive-family obligation after accepted #279; it does not resubmit its finite counting theorem. Work in dimension d=n+1. Coordinates are ordered in reverse relative to the usual Klee–Minty presentation. The public type uses only Mathlib symbols and explicitly lists the original box and neighboring-coordinate inequalities.

For a Boolean word b, recursively form its tail point and prepend e*s or 1-e*s according to b(0), where s is the tail's first value (zero for an empty tail). Every scalar value is in [0,1]. The two branch ranges lie in [0,e] and [1-e,1], which are disjoint for 0<e<1/2. Recursion therefore proves injectivity of the scalar encoding, then of the entire point map. Its image has exactly 2^d points.

The new extremality proof does not assume an active matrix, a vertex list, or a support oracle. If one constructed point is a strictly positive convex combination of two feasible vectors, project to the tail. Induction forces both tails to equal the prescribed tail point. The first coordinates lie in the same closed interval, and their convex combination equals one endpoint of it. Positivity of both coefficients forces both first coordinates to that endpoint. This proves Mathlib extremePoints membership against arbitrary feasible endpoints, not just other listed points.

For each (d-1)-bit tail, the two possible new head choices differ by (1-2e*s) times the first coordinate vector. This coefficient is positive; scalar injectivity makes those coefficients pairwise distinct. Both endpoints belong to the constructed V. The accepted #279 proof (included verbatim except its root name and axiom-print name) supplies a detecting coordinate for every injective affine embedding and 2*card(tails)<=K(K-1). Finite Boolean-function cardinalities yield 2^d<=K(K-1).

The theorem thus constructs a genuine exponential set of extreme points and its all-embedding coordinate obstruction without a supplied displacement family. It does not classify every edge or assert that every extreme point is listed. Neither fact is needed for the stated lower bound on the coordinate image of this constructed set. In particular, large coordinate-level inventories are not graph-diameter lower bounds. The classical short routes and broader Polynomial Hirsch question remain distinct.

The parameter is any real e in the open interval, not a finite rational test set. Dimension one (empty tail) is included. The standalone file contains the prior accepted finite theorem plus the new recursive construction and extremality chain, with five final axiom printouts in total. The public target has no locally declared preamble definitions, no assumed desired count, and no self-import.

Local Lean/Lake is absent and release-host DNS fails in this runtime. The candidate therefore requests one prepared exact-head comment gate for compilation, transitive-axiom auditing and publication. Source examination and rational tests are not Lean verification. Preserve any compiler diagnostics and stop rather than using repeated speculative Actions edits. The committed toolchain, actor allowlist, duplicate checks and verify/publish secret separation are unchanged.
