# Target-only two-level original rows give a Hirsch route

Let P be exactly both the convex hull of the supplied finite real set C and the intersection of the ORIGINAL m linear inequalities A_i(x)<=b_i in ambient dimension d. Fix actual extreme points u and v. The structural condition is imposed ONLY on labels tight at v: on all actual vertices, A_i takes its boundary value b_i and at most one other value lo_i. The other rows need not have two levels. No simplicity, strict feasibility, full dimension, complete vertex list, active basis, neighbor graph, short path or numerical separation is a premise.

The proof first derives the active-row kernel criterion. If a vector z is annihilated by every row tight at an actual vertex x, a finite positive slack margin makes both x+e*z and x-e*z feasible. Their midpoint is x, so extremality forces z=0. Thus the active evaluation map is injective. Finite-dimensional linear algebra gives at least d active original labels. Applying the same kernel criterion at v shows that all its tight rows determine v uniquely.

For a set S of labels tight at v, filter C by all equations in S. Its entire convex hull is an extreme subset of P. The induction starts with P and uses the accepted exact attained-support-face theorem from #322 at each inserted original row. This proves actual retained-face geometry, not a claimed feasible vertex subset. At the current vertex x, choose S to contain exactly the target rows already tight at x. Both x and v belong to the retained finite hull, and x remains extreme there.

Unless x=v, some target row j is still slack. Inside the retained face, the accepted #322 improving-edge construction applied to A_j gives an adjacent actual vertex w with A_j(w)>A_j(x). That result constructs an entire exposed segment of the retained hull, not a chord or a supplied neighbor. Since A_j has only the boundary value and one other value on original vertices, improvement forces A_j(w)=b_j in ONE original step. Every row in S remains tight by face membership. Extremality of the whole segment transfers from the retained face to the original P.

Consequently the set of target labels missing at the current vertex strictly decreases on every edge. Strong induction constructs the complete indexed route, with length at most the INITIAL missing-label cardinality. Those missing labels are disjoint from the source's active labels. At least d of the m original labels are already active, giving L<=m-d. Both inequalities and explicit per-step target preservation/acquisition are in the public theorem. The target-active rank and unique endpoint are conclusions of the geometry, not supplied oracles.

All conclusions concern the SAME ORIGINAL H body. The public edge predicate is a distinct endpoint pair whose entire segment is Mathlib IsExtreme; local exposure is used internally and no separate ambient-IsExposed strengthening is asserted. Dimension zero, equal endpoints, lower-dimensional feasible hulls, redundant original rows, nonsimple vertices and redundant/interior generators are retained. The finite H/hull equality is an exact structural premise; this is not an efficient H-to-V conversion theorem. m counts DISPLAYED original inequalities, not an independently formalized irredundant facet lattice. Non-target-facet nonrevisiting and shortestness are not asserted.

This is a restricted positive class theorem with an ORIGINAL m-d bound, unlike #322's bound in a possibly exponential coordinate inventory. It does not prove that arbitrary carriers have the target-only two-level property. In particular, a six-vertex hexagon in the exact tests has an endpoint pair at distance three with neither source neighbor acquiring a target row; its target rows have more than two levels. This defeats dropping the structural condition from the one-step claim, not Polynomial Hirsch. Two-level polytope geometry and finite active-row arguments are classical; no historical-priority or best-known-bound claim is made.

Reuse is source-exact: accepted #322 lines 1-101 retain its Route operations; lines 359-804 retain the complete finite perturbation, strict separation, improving-edge and support-face code. Only closing namespace commands are added around these selected ranges. Old public targets and their route/level conclusions are not resubmitted. The new TargetRows section and root assembly supply the original-H rank, locking, acquisition and counting arguments. All five frozen dependency hashes were recomputed from the accepted archive. The public preamble is import/open/options only, with the finite count predicate explicitly classically decidable.

Supporting execution uses the unchanged #322 exact reference/edge implementation and one new target-row producer/consumer. Eighteen small hull models cover 70 qualifying targets and explicitly reject 36 other targets. All 456 ordered routes and 565 original-edge occurrences pass, including affine transforms, redundant/interior generators, nonsimple/lower-dimensional hulls and redundant/rescaled rows. Three nonshortest outputs are retained. Lower-dimensional H tables include both signs of affine-hull equations, so they really define the same hull; omitting them is rejected. The route producer never reads the reference edge table. The independent consumer checks full support faces, all original target rows, every acquisition and both route bounds, and replays saved certificates with the edge producer disabled. Twelve malformed cases fail. These are exact supporting tests, not a Lean proof of Python, JSON parsing, complete general vertex enumeration or large-dimensional execution.

Local Lean/Lake and cached dependencies are unavailable, and toolchain-host DNS fails. Static source identities, the exact public-type transcription and rational tests are NOT Lean compilation. This complete packet is prepared for one normal pinned compiler/axiom/publication gate. Any failed elaboration must remain visible; do not weaken the statement or use repeated speculative hosted edits. The Lean/Mathlib pin, strict 0.10.7 publisher, duplicate guards, allowlist and verifier/publisher credential split are unchanged.
