# Construct a first-blocking original edge, not a supplied neighbor

For d<m and distinct real moment parameters, the accepted active-evaluation theorem constructs a vector w taking value -1 on a chosen tight row p and zero on all other currently tight rows. All mean-centered rows sum to zero on any vector, so at least one original row has positive slope on w. Such a row is not already active. Its source slack is strictly positive.

Take the minimum of (1-row_i(u))/row_i(w) over positive slopes. This is a nonempty finite set of positive numbers, so the minimizing step t exists and is positive. Rows of positive slope stay feasible up to t by the ratio inequality; rows of nonpositive slope remain feasible automatically. A minimizing q becomes tight. Conversely q excludes every s>t. Thus the entire nonnegative feasible ray interval is exactly [0,t].

The old active set has d elements. The endpoint retains its d-1 rows other than p, releases p strictly, and gains q. The accepted bound of at most d tight rows at EVERY ambient point forces this to be the entire endpoint active set, with no simultaneous extra blockers. The accepted vertex criterion yields a new actual extreme point. The accepted #295 common-row theorem identifies the WHOLE common equality slice with the nondegenerate exposed original segment. This is not a projection of an auxiliary edge.

The standalone source reuses the full accepted #295 namespace prefix byte-for-byte and adds the finite-ratio, direction and endpoint construction. Its public preamble contains only imports/open/settings, with original row/P/active formulas inlined as lets. Five transitive axiom printouts cover the new chain and accepted active-evaluation theorem. No support/rank/direction/neighbor/step/exchange oracle is a premise. The arbitrary tight row is a choice of which edge to follow, not a supplied feasible direction.

This works from any actual moment vertex, unlike a construction restricted to consecutive root blocks. It supplies a local pivot, not a policy producing a polynomial number of pivots, a whole graph, a shortestness claim or Polynomial Hirsch. Zero dimension is vacuous because no active row exists. Dimension one is included; not all m represented rows are asserted genuine facets in every boundary case. Classical simplex geometry is credited rather than claimed historically novel.

Local Lean/Lake is unavailable and the toolchain-host DNS preflight failed. Source and exact rational regression are not compilation. This is one prepared new-comment final compile/axiom/publication gate. Retain any actual failure instead of speculative hosted edit iteration. The committed Lean/Mathlib pin, protocol, duplicate guards, workflow, allowlist and verify/publish credential split remain unchanged. #296/#297 and all other owned work, including reserved #210, are untouched.
