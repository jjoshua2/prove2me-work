# Direction inheritance in every compact-summand completion

Let P=conv(C), with C finite, and assume the actual equality P+Q=Z for a nonempty compact Q and a coefficient segment sum Z. Consider a nondegenerate whole exposed edge [u,v] of P. Its direction D=v-u must be parallel to a nonzero generator of Z.

Suppose otherwise. Project linearly transverse to D, then reuse the accepted finite regularization theorem on the projected generator vectors and projected differences u-c for every c in C. The original exposing objective annihilates D and has strictly positive gaps at all corner points outside the edge. The new objective, composed with the projection, still kills D and keeps those gaps positive, but is nonzero on every nonzero zonotope generator. Both u and v maximize it on P; compactness selects a maximizing q in Q. Thus both u+q and v+q maximize on Z, contradicting the accepted regular-objective singleton-face theorem. This is an argument in objective space, not a claim that projecting a polytope edge preserves adjacency.

Apply this result independently to each selected original edge direction. The selected parallel generators cannot coincide or be parallel for distinct input directions. This proves the generator injection and invokes the unchanged accepted #317 lower bound to construct opposite completion vertices requiring at least r genuine original exposed-edge steps. The selected structural edge family and the sum equality remain inputs; the central direction-inheritance property is proved, not assumed. Q convexity is not used, though ordinary compact convex summands are included.

The accompanying written note gives a separate all-dimensional application: a triangular deformed cube has 2d genuine original facets and diameter d, but exactly 2^d-1 distinct edge directions. Consequently EVERY compact-summand zonotope completion has global diameter at least 2^d-1. The note proves the exact vertex/edge description, direction count and translation reduction, with complete small H checks. That application is NOT an additional Lean theorem inside this target. It rules out uniformly polynomial GLOBAL zonotope-completion diameter, not fibre-sensitive contraction, non-zonotopal completions or Polynomial Hirsch.

The 629-line accepted #317 namespace prefix and the 81-line accepted #313 regularization namespace/helper prefix are copied byte-for-byte; old public roots are excluded. No accepted result is resubmitted. Classical Minkowski and zonotope geometry is credited to Deza--Pournin and related literature; no historical-priority claim.

The original candidate was local-only and uncompiled. This continuation preserves the exact proof and target, and prepares its first real PR-comment verification/publication gate. Local Lean/Lake is unavailable; source consistency and exact rational tests are not Lean verification. The complete file must pass the pinned Lean 4.30.0 / Mathlib c5ea00351c28e24afc9f0f84379aa41082b1188f compile and transitive axiom audit before any platform submission. The actual result belongs in separate receipts and the current handoff, not inferred from these preparation notes. No version guard, workflow, actor allowlist, duplicate safeguard, secret separation or reserved #210 changes are authorized by this packet.
