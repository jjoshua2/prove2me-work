# Positive original-edge routing, not another counting obstruction

This packet uses the accepted #280 TriangularFamily helper block unchanged and proves the missing positive geometric counterpart. The public statement quantifies over arbitrary actual extreme points of the explicit original inequality system, not only endpoints already supplied with Boolean labels.

At each recursive step, a feasible tail x permits a head coordinate between e*lead(x) and 1-e*lead(x). Both endpoint maps are affine. The new proof shows that an extreme subset of the tail polytope lifts to an extreme subset on either boundary. A supporting inequality is saturated by a positive convex combination only if both endpoints saturate it; projecting to the tail then gives the asserted extreme-set membership. Mapping a segment under this affine lift produces exactly the segment joining its lifted endpoints, so real tail edges remain original edges.

The entire vertical fibre over an extreme tail is also an extreme segment. Its endpoints are distinct since 0<e<1/2. A separate converse classification proves every extreme point has a head at one of these two boundaries and an extreme tail: an interior head would lie in an open vertical segment, and any nontrivial convex decomposition of a tail would lift to one of the full point. Induction therefore produces the endpoint labels, rather than assuming a complete vertex list.

To connect two recursively labelled points, change the head boundary once only when needed, then lift an inductively constructed tail route along the target head boundary. This costs at most one plus the tail dimension. The final path stays among actual extreme points and all transitions are nondegenerate extreme segments in the full original set. Dimension zero and equal endpoints are included. This proves an ordinary-edge upper bound for this class, not an auxiliary graph route or projected chord.

The full exponential affine-level obstruction from #280 is not republished. Its earlier point-construction/feasibility/extremality helper proofs are reused byte-for-byte. The new edge-lifting, full extreme-point classification and bounded path recursion are separate obligations. The classical family has a known cube graph; no historical novelty is claimed. This formal positive result does not establish a polynomial bound for arbitrary high-dimensional carriers.

Local Lean/Lake is absent and toolchain-host DNS is unavailable in this runtime. The prepared standalone packet therefore requires the existing exact-head comment gate. Source checks and exact rational regression are not compilation. Five final axiom printouts cover the new lifting, vertical fibre, classification, route recursion and public theorem. Compile/audit before publication, preserve diagnostics on failure, and do not use repeated speculative Actions runs as an edit loop. No pin, workflow, duplicate guard, allowlist, credential, secret separation, or reserved branch is changed.
