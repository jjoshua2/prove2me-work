# From a regularized objective sweep to a genuine original-edge walk

The accepted #313 theorem supplies a perturbed objective k, with the same whole target face as g, and an exact finite set T of exceptional times in (0,1). Each exception exposes a whole original nondegenerate edge; all other times expose a singleton vertex. It does not itself assemble those faces into a vertex walk. This packet constructs that missing walk rather than assuming an ordered event sequence or adjacent vertices.

Insert 0 and 1 into the ACTUAL finite crossing set and enumerate the resulting set increasingly with Mathlib orderEmbOfFin. The cardinality is |T|+2, its endpoints are 0 and 1, and every interior entry lies in T. Exact finite-order arguments show no other event lies between consecutive entries. Take an interior sample (the midpoint) of each complementary interval. Its objective is regular, so its canonical coefficient-saturated point is an actual extreme point and its whole maximizing face is a singleton.

A continuous affine evaluation on a generator that has no zero in a closed interval has constant sign, by the pinned intermediate value theorem. This identifies the first and last chamber faces with the requested faces of f and g. It also places each chamber vertex in the whole event face at either end of its interval: every generator not tied at that boundary keeps its saturated coefficient. The original zero/parallel/opposite/repeated generators are retained.

The two vertices bracketing a genuine crossing are distinct. A nonzero tied generator has opposite nonzero signs at the two samples. Were the resulting points equal despite a redundant generator representation, coefficient saturation for the first regular objective, applied to the second representation, would force those two generator picks to agree. This contradicts the weighted affine evaluation identity at the crossing. Thus distinctness is derived without assuming an injective Boolean-cube image.

Both adjacent chamber points are actual extreme points of the ambient zonotope and lie on the whole exposed event segment. An ambient extreme point on a feasible segment is one of its endpoints. Since the two chamber points differ, they are exactly its two endpoints (in either order), so the event segment is their actual original exposed/extreme edge. There is one transition per distinct event; hence the constructed Fin(|T|+1)-indexed vertex walk has length |T|<=m.

The complete accepted #313 namespace prefix is reused byte-for-byte, including #312 whole-face geometry. Only its old standalone public solution and final printouts are omitted. The five frozen hashes from its original verified archive were checked. No accepted theorem is resubmitted. The new public preamble contains imports/opens/settings only, with the zonotope and support faces inlined as lets and a matching top-level solution. Five transitive axiom printouts cover the event mesh, distinct crossing vertices, segment identification, full route, and public theorem.

This is a classical generator-linear zonotope route formalization, not a new best diameter theorem or a proof of unrestricted Polynomial Hirsch. BOTH regular endpoint objectives remain explicit inputs. Constructing such objectives for arbitrary specified extreme points remains separate. The number m counts segment generators, not original H facets; no polynomial original-input generator budget is asserted. No shortestness, monotone objective values, or executable real algorithm is claimed. Zero dimension, empty generator families, zero events and redundant/antiparallel generators are included.

Local Lean/Lake and compiler-host DNS were unavailable. Exact rational tests and byte-identical source checks are not Lean verification. This complete candidate receives one prepared new top-level PR conversation publication comment through the existing pinned gate; any compiler failure must remain preserved rather than starting a speculative hosted edit/compile loop. Workflows, strict 0.10.6 guard, allowlist, duplicate protections, secret separation, other owned work and #210 are unchanged.
