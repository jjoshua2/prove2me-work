# One-shot affine diameter publication request

Publish only `Hirsch.injective_affine_image_diameter_iff` from verified source commit `be54654435d3c317d676f3ea03c79d07f089992a`, source verification run `34611873550`.

The workflow rebuilds the exact standalone source plus target wrapper and audits `solution` before exposing the Prove2Me credential. It reuses the immutable reviewed client from `1692538ba9f7661ca079a86f2963fb48ac2270d1`, with exact version guard 0.10.1, theorem-name/type collision checking, proof-byte comparison, ACCEPTED plus live Proved requirements, and unchanged d>=4 frontier checks.

No conjectural child or conjecture decomposition is requested. Record the resulting receipt on the source branch and close this publication PR unmerged. Remove the one-shot workflow from the source branch before merging PR90.
