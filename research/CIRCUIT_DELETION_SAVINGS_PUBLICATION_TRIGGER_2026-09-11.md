# Publication trigger — exact circuit-carrier deletion-savings identity

This publication-only marker exists solely to synchronize PR #126 after the owner-only, fail-closed publication workflow was aligned with the frozen standalone publisher on `main`.

The workflow must compile and axiom-audit the exact standalone `solution` before `PROVE2ME_API_KEY` is exposed. This branch is not intended to be merged.
