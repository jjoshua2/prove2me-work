# Credentials — never commit tokens
credentials.json

# Lean build artifacts (the env pin files ARE committed in this personal copy)
lakefile.olean
.lake/
*.olean

# OS noise
.DS_Store
*:Zone.Identifier
**/*:Zone.Identifier
