# Current Polynomial Hirsch frontier

Repository `jjoshua2/prove2me-work`; refreshed September 14, 2026 UTC.
Keep Lean `v4.30.0` / Mathlib `c5ea00351c28e24afc9f0f84379aa41082b1188f`.
Read AGENTS.md, CLOUD_AGENT.md, SKILL.md, CONTINUE_HIRSCH.md and LIVE PR
heads/comments before choosing work. Do not resubmit accepted/pending packets.

The complete preceding frontier is preserved VERBATIM in
[the pre-tangent overview](research/FRONTIER_BEFORE_IMAGE_TANGENT_SELECTION_2026-09-14.md),
Git blob `0013f957c727331fff67ba3adc6ce30cad70b80a`. It includes the newly
accepted #241, accepted #247, their exact evidence, all older accepted chains
and archives. No prior theorem ID, limitation or ownership is discarded.

## Root target and unchanged global obstacle

The last preserved authenticated audit reports root
`58eae2c9-6fd5-4d5d-8aa7-6d552ad80bac` Open, six curated milestones Proved,
and leaf `Hirsch.common_face_diameter_of_dim_ge_six`,
`87a8b4f4-8b58-4340-8cb9-5fd1b548d01e`, Open. This is NOT a new root/leaf poll.
No result here proves a uniform polynomial number of ORIGINAL IMAGE edges for
arbitrary high-dimensional carriers. Strict objective improvement gives finite
termination on a polytope, not a polynomial bound.

The selected-portal framework and dimension/excess accounting remain available.
A factor-three sibling mass bound alone does not solve a polynomial recurrence.
Exact allocation tests do not discover useful decompositions for every carrier.
Auxiliary positive circuits are not original edges; source edges can project
to interior chords. Conjectural cost must use ORIGINAL IMAGE facet counts,
not the sometimes smaller row count of an extended formulation.

## Accepted interfaces to reuse, not reopen

The archived checked-allocation chain includes #216, #218, #219, #221, #222,
#224, #227, #229, #230, and #233--#237. #236 ALREADY composes the actual rational
checker, bound allocation matrix, whole-set geometry and sharp support budgets.
#237's coefficient recession theorem is not automatically an image-boundedness
statement. Python/JSON producers are not Lean-extracted by passing exact tests.

Geometric/representation interfaces:
- #239/#240: additive envelope count and actual exposed core-edge bridges.
- #241: ACCEPTED and merged bounded-image compact representatives, preserving
  original zero coordinates and tight resources. Theorem
  `45f5569d-e24c-4a42-9572-de74ca0629c0`, submission
  `0a3332a7-8c22-4fe4-90c1-9fd858b95a71`. Packet bounded_image_representatives.
  It does not make projection edge-preserving or prove a polynomial diameter.
- #242/#243: constructed generic endpoint objectives and chronological finite
  crossing sequence, including boundary maxima, stationary compression/count.
- #244: ACCEPTED constructed fixed-core full-sum fibre route. Theorem
  `2f4c0ca7-8412-47e8-8ab6-4d44b8699ee5`, submission
  `24b8a2c2-6a38-4f35-89e7-6b85f4d0b87c`. Its owner retains the distinct
  core-walk CONCATENATION. Inspect live state, do not duplicate publication.
- #245: ACCEPTED uniform endpoint objectives for SAME #240 bridge endpoints.
  Theorem `0918de00-86f9-4f1d-9e20-7d1dc36efa69`, submission
  `217a1321-8d13-4d62-894c-328c889c7383`. The old unpushed packet is closed work.
- #246: ACCEPTED original-row image-edge certificate, including nonvertex lifts
  and unbounded/high-dimensional source faces. Packet projected_face_edge_certificate.
- #247: ACCEPTED minimal projected face discovery from exact fibre duals.
  Theorem `2e10b83d-9a75-4d41-a7e5-95a483d787aa`, submission
  `4f1c0864-fd8d-47d1-96b6-2c823601c987`. Merged3a23096b7b0938d5fb7c5aec65ba61cd797d65fb.
  Its producer decides a SUPPLIED pair using actual positive/negative geometric
  witnesses; it constructs the face and exposer, not a route-length bound.
- #248: NEW ACCEPTED normalized tangent image-to-original-edge implication,
  with a general finite-success improving-edge selector and route producer.

## NEW acceptance: normalized tangent image edges, #248

`Hirsch.normalized_tangent_slice_lifts_to_image_edge` is ACCEPTED with trusted
publisher authenticated live Proved. Theorem
`b531d260-eff2-475a-b25b-e7c941a21ec6`, submission
`cdeaac97-9b6c-4b7b-aba8-2ec80f32efef`. Frozen proof head
`7d3c7cfcbbb4a3b7d4eda9aa728330592faf1770`, run `34900402935`.
[Accepted evidence](research/publication_packets/normalized_tangent_image_edge/accepted-evidence.md),
raw audit/log/manifest/publication receipt and derived archive readback are in
the packet. The285-line proof passed its FIRST gate and FIRST platform submission
unchanged. All five axiom reports contain only propext, Classical.choice and
Quot.sound; all three compile exits are zero. Both downloaded archives and all
five frozen hashes were independently checked. Do not resubmit it.

For a feasible anchor tight exactly on J, finite strict slacks prove

    G{d:a_J d<=0} = {s*(Gz-Gx):s>=0,z feasible}.

Positive active-row weights and a finite image-row factorization give a height
positive on every nonzero IMAGE direction despite source lineality. From a
uniquely exposed point r of the height-one tangent image and an attained maximum
tau over ALL source lifts on that image ray, construct ell=f-f(r)h. Its whole
support slice is the nondegenerate segment [Gx,Gy], an actual IsExtreme subset,
and the target objective strictly improves. No original-image edge/exposer,
source vertex/edge, compact source or one-dimensional source face is assumed.

Normalized-slice support and all-lift maximality ARE witness interfaces of the
formal theorem. Software produces them with #247-style fibre duals and exact
LP certificates; the entire algorithm/parser/LP termination is not yet a single
Lean-extracted theorem. Do not transfer generic acceptance to arbitrary JSON.

### Executable route selection, no proposed neighbors

    mkdir -p fixtures
    python3 scripts/test_image_tangent_pivot.py
    python3 scripts/image_tangent_pivot.py input.json --output route.json

Input only A,b,G,u,v, with image endpoint vertices. The selector derives the
strict target objective, certifies bounded IMAGE coordinates, and repeatedly
optimizes its normalized image tangent slice. Image-coordinate lexicographic
refinement and a singleton face certificate isolate a ray. The endpoint LP
maximizes over ALL lifts, not one source line. The resulting route is strictly
monotone. Stationary u=v is included; caps report incomplete work, not negative
geometry or a polynomial route theorem.

Mandatory controls: an optimal square-slice midpoint yields a diagonal, so a
plain optimal direction is insufficient; a fixed source ray can stop at1 when
the image edge continues to2 by changing lifts. Free source coordinates can
remove every source vertex. All are handled without assuming source adjacency.
A containing hull conv{-W_i/lambda_i} bounds the normalized image, but need not
equal it; NEVER infer an m-ray or m-step bound from that containment alone.

Executed297 routes/428 image edges. Nine small independent hulls have49 vertices,
62 edges,291 ordered distances and1012 incident-edge normalized-slope checks.
Thirty-two source edges project to nonedges. Two pentagon routes are nonshortest.
Six additional examples include source lineality, paired-coordinate cube images,
a2^-120 thin projection, a lower-dimensional image and dense source coordinates;
they yield20 edges,12 premature fixed-source blockers and18 nonedge source-lift
steps. Twenty malformed/capped cases fail. The auditor succeeds with all LP,
elimination and discovery entry points disabled. These are not shortestness or
separately kernel-evaluated claims. Clean replay reproduces all result fields
except timings and all seven fixtures byte-for-byte. IMAGE_TANGENT_EXECUTION.json
is a labeled derived summary; the script regenerates full stage records.

## Exact next task and ownership

The image line now SELECTS actual improving original-image edges rather than
merely recognizes a supplied pair. The next conjecture-facing task is a valid
amortized progress bound on their total number, or a demonstrably better edge
selection rule. Geometry-dependent normalizations may change between vertices;
strict improvement alone has no uniform quantitative guarantee. Keep facet
counts on the original image, not the extension. Stress-test long monotone
routes, degeneracy, changing lifts and normalization scaling before asserting
a decreasing bounded potential. See [handoff](research/IMAGE_TANGENT_SELECTION_HANDOFF.md).

#244 retains the independent core-bridge/fibre concatenation. Its intended
supplied-model bound is L+(L+1)K; #245 supplies compatible endpoint objectives.
No useful controlled decomposition for every carrier is proved. #238 owns
strict original-H support-witness existence. #241 is already accepted/merged,
not a blocked unfinished task. The old #208 submission is not freshly polled
and must not be retried from this index. #210 is retired historical ancestry.
Inspect #248's live evidence integration/merge state instead of rerunning its
accepted packet; its post-gate additions do not alter accepted source/metadata.

## Execution discipline

Use local pinned Lean when available. This runtime lacked Lean/Lake and public
toolchain DNS; source/Python tests were not claimed as compilation. Prepared
hosted gates are not speculative edit/compile loops. Only the trusted main
publisher uses credentials; no workflow, pin, permission or secret isolation
changes. Preserve exact raw receipts separately from summaries, and distinguish
compilation, axiom audit, authenticated ACCEPTED and live Proved.
